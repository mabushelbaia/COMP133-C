#include <stdio.h>
#define SIZE 4
void calculate(int kilosused[],int cost[]);
int main(){
    int kiloused[SIZE]={725,115,600,327},usernum[SIZE]={123,205,464,596},cost[SIZE];
    calculate(kiloused,cost);
    return 0;
}
void calculate(int kilosused[],int cost[]){

}
